import './App.css';
import {BrowserRouter as Router, Switch, Route} from 'react-Router-dom';

function Routing(){
    return (
        <BrowserRouter>
            <Router>
                
            </Router>
        </BrowserRouter>

    );
}